"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.store = void 0;
const redux_1 = require("redux");
const redux_thunk_1 = __importDefault(require("redux-thunk"));
const immutable_1 = require("immutable");
const reducers_1 = __importDefault(require("../state/reducers"));
const constants_1 = require("../config/constants");
const configureStore = () => {
    const initialStoreState = (0, immutable_1.Map)();
    const enhancers = [];
    const middlewares = [
        redux_thunk_1.default,
    ];
    let devtools;
    if (constants_1.__DEBUG__) {
        devtools = (typeof window !== 'undefined'
            && typeof window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__ === 'function'
            && window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__({ actionsBlacklist: [] }));
        if (devtools) {
            console.info('[setup] ✓ Enabling Redux DevTools Extension');
        }
    }
    const composedEnhancers = devtools || redux_1.compose;
    const storeEnhancers = composedEnhancers((0, redux_1.applyMiddleware)(...middlewares), ...enhancers);
    const store = (0, redux_1.createStore)(reducers_1.default, initialStoreState, storeEnhancers);
    return store;
};
exports.default = configureStore;
exports.store = configureStore();
